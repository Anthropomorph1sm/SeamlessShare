# Set this to the server's fixed LAN IP, or a local DNS name that all devices resolve.
SHARE_HOST=192.168.1.50
# Optional for compose.hub.yaml; use a published version such as 1.2.3 to pin a release.
SHARE_IMAGE_TAG=latest
